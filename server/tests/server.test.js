//this for testing purpose only 

const expect = require('expect');
const superTest=require('supertest');
// here app ,emp is the local variable for this file and using these variable we can make the use of 
//properties exported properties and function its same as import 

const {app}=require('../server');
const{emp}=require('../model/emp');
